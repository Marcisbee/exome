{
  "name": "exome-benchmark",
  "version": "0.0.0",
  "scripts": {
    "build": "esbuild index.ts --sourcemap --minify --target=es2016 --platform=browser --format=esm --bundle --outdir=dist --define:process.env.NODE_ENV=\\\"production\\\"",
    "test": "npm run build && npx serve"
  },
  "devDependencies": {
    "jotai": "^0.16.0",
    "mobx": "^6.2.0",
    "mobx-react": "^7.1.0",
    "react": "file:../node_modules/react",
    "react-dom": "file:../node_modules/react-dom",
    "recoil": "^0.2.0",
    "valtio": "^1.0.3"
  }
}
